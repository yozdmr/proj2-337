name: proj2-337
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pip
  - setuptools
  - wheel
  - pip:
      - numpy
      - pandas
      - requests
      - flask
      - flask-cors
      - beautifulsoup4
      - spacy
      - nltk
      - python-dotenv
